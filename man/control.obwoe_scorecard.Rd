% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obwoe_scorecard.R
\name{control.obwoe_scorecard}
\alias{control.obwoe_scorecard}
\title{Control Parameters for the Scorecard Pipeline}
\usage{
control.obwoe_scorecard(
  corr_cutoff = 0.7,
  corr_method = "pearson",
  pdo = 20,
  score_ref = 600,
  odds_ref = 50,
  direction = "higher_is_safer",
  n_groups = 10L,
  na_woe = 0,
  drop_negative = TRUE,
  max_abs_coef = 15,
  min_events_per_variable = 20,
  engine_fallback = FALSE,
  overwrite = TRUE,
  digits = 6L
)
}
\arguments{
\item{corr_cutoff}{Numeric in \eqn{(0, 1]}. Absolute correlation in the WoE
space at or above which two variables are treated as redundant. Default
\code{0.70}. Set to \code{1} to disable pruning.}

\item{corr_method}{Character string passed to \code{\link{obcorr}}. Default
\code{"pearson"}.}

\item{pdo, score_ref, odds_ref, direction}{Passed to \code{\link{obwoe_scale}}.}

\item{n_groups}{Integer. Number of score bands in the gains and stability
tables. Default \code{10}.}

\item{na_woe}{Numeric. WoE given to a value that falls in no fitted bin —
an unseen category, or a missing value where the binner built no missing
bin. Default \code{0}, which is the population-average contribution.}

\item{drop_negative}{Logical. Refit without any variable whose coefficient
on WoE comes out negative? Default \code{TRUE}. A negative coefficient
means the variable is fighting the rest of the model, and a scorecard with
one cannot be signed off.}

\item{max_abs_coef}{Numeric. Coefficients above this in absolute value are
flagged as separation. Default \code{15}.}

\item{min_events_per_variable}{Numeric. Events per variable in the model
below which the fit is flagged as thin. Default \code{20}, the usual
rule of thumb.}

\item{engine_fallback}{Logical. If the requested engine's package is not
installed, fall back to \code{"glm"} with a warning instead of failing?
Default \code{FALSE}.}

\item{overwrite}{Logical. Overwrite \code{file} if it exists? Default
\code{TRUE}.}

\item{digits}{Integer. Rounding used in the workbook. Default \code{6}.}
}
\value{
An object of class \code{"obwoe_scorecard_control"}.
}
\description{
Settings for the stages of \code{\link{obwoe_scorecard}} that have no home
in \code{\link{control.obwoe}} or \code{\link{obwoe_select}}.
}
\examples{
control.obwoe_scorecard(pdo = 25, score_ref = 700, corr_cutoff = 0.8)

}
\seealso{
\code{\link{obwoe_scorecard}}
}
