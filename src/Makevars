# Set C++ standard
CXX_STD = CXX17

# Add current directory to include path
PKG_CPPFLAGS = -I.

# Optimization for smaller binary size (CRAN NOTE fix)
# -Os: Optimize for size (safe, standard flag)
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) $(ARCH_SPECIFIC_CXXFLAGS) -Os

# Combine all flags
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(ARCH_SPECIFIC_LIBS)